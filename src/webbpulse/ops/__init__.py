"""Operator tooling that runs from a workstation against a deployed environment.

Nothing here is imported by a service at runtime. `webbpulse.ops.config` backs the
`webbpulse-config` console script.
"""
