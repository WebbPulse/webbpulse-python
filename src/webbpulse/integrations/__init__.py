"""Clients for third-party services a product integrates with.

Each integration is its own module behind its own extra, so importing this package pulls in
nothing: `webbpulse.integrations.github` needs the `github` extra.
"""
