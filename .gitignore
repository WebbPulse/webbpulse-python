__pycache__/
*.py[cod]
.venv/
venv/
build/
dist/
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/
# uv reads the [dependency-groups] table and writes this, but the project pins nothing:
# CI and the README both install with `pip install -e`, so a committed lockfile would go stale.
uv.lock
